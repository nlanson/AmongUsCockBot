export interface botCredentials {
    userAgent: string;
    clientId: string;
    clientSecret: string;
    username: string;
    password: string;
};

export interface cockList {
    cocks: Array<string>;
};
